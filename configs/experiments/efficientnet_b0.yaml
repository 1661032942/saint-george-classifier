data:
  batch_size: 16  # memory-driven cap (8 GB RAM; batch 64 OOMs on CPU)
# E2 — Backbone comparison: EfficientNet-B0 (same protocol as E1).
model:
  backbone: efficientnet_b0
training:
  epochs: 6
  early_stopping_patience: 3
  lr: 0.0003
augment:
  randaugment: false
  cutmix_mixup: false
