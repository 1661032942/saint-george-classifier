# E1 — ResNet18 full run: longer schedule on the validated pipeline.
# Single-variable comparison vs E0 (epochs 8 -> 16, patience tightened).
model:
  backbone: resnet18
training:
  epochs: 16
  lr: 0.0003
  early_stopping_patience: 6
augment:
  randaugment: false
  cutmix_mixup: false
